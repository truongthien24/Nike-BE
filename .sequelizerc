const path = require("path");

// Tạo ra mô hình MVC
module.exports = {
  "config": path.resolve("./src", "config.json"),     //Kết nối được tới database
  "migrations-path": path.resolve("./src", "migrations"),   //Cách tạo các bảng trong database
  "models-path": path.resolve("./src", "models"),    
  "seeders-path": path.resolve("./src", "seeders"),    //Tạo dữ liệu fake
};
